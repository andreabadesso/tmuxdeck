# TmuxDeck Bridge Configuration

# Required — WebSocket URL of the TmuxDeck backend
BRIDGE_URL=ws://your-server:8000/ws/bridge

# Required — create a bridge in TmuxDeck Settings to get a token
BRIDGE_TOKEN=

# Optional — display name (defaults to container hostname)
BRIDGE_NAME=

# Host tmux socket directory to mount into the container
# Linux:  /tmp/tmux-$(id -u)
# macOS:  check `echo $TMUX | cut -d, -f1 | xargs dirname`
TMUX_SOCKET_DIR=
